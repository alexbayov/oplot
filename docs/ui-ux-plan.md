# План UI/UX: юзабельный интерфейс — Issue #79

> Ревизия 1. 2026-05-26.
> Цель: превратить интерфейс из «текста и прямоугольников» в юзабельный UI в стиле Military Graphic Novel.
> Все изменения — только `src/scenes/*.ts`. Игровая логика, баланс, контент не трогаются.

## 0. sceneUi.ts — база (затрагивает все сцены)

Файл: `src/scenes/sceneUi.ts`

| Элемент | Сейчас | Будет |
|---|---|---|
| `createTitle` | Arial 32px, #F5F1E8 | Impact 28px, #D4C5A0 |
| `createSubtitle` | Arial 16px, #C8C0B0 | Arial 14px, #C8C0B0, line-height tighter |
| `createPanel` | #2a2a2a, обводка 1px #5f5a50 | #2d2d2a alpha 0.92, обводка 2px #4a4a3a |
| `createButton` | #3f493d, 42px высота, без обратной связи | #2d2d2a alpha 0.95, 48px высота, hover→#c5a267, press→#4a3a2a, tween-анимация |
| `createHpBar(x,y,cur,max,w,h)` | — (нет) | Красная полоска #8b0000 на тёмном фоне #3a0000, обводка #4a4a3a. Возвращает `[bg, bar]` |
| `createSmallButton(x,y,label,w)` | — (нет) | 36px высота, ширина параметром, для боевых действий |

Палитра (из style-guide.md):

- Фон: `#1a1a1a`
- Панель: `#2d2d2a`
- Обводка: `#4a4a3a`
- Заголовок: `#d4c5a0`
- Текст: `#c8c0b0`
- Акцент: `#c5a267`
- Опасность: `#8b0000`
- HP-фон: `#3a0000`

Подписи на кнопках — 16px, заголовки — 28px, основной текст — 14px.

---

## 1. BaseScene.ts — главный экран «Оплот»

Файл: `src/scenes/BaseScene.ts`

**Layout:**

```
┌──────────────────────────────┐
│           ОПЛОТ               │  ← createTitle
│                               │
│  ┌──────┐  HP ████████░░ 100  │  ← hero спрайт (x=50, y=250, scale 0.8)
│  │ hero │  Уровень 5          │  ← HP-бар (createHpBar, 200×10)
│  │      │  Оружие: Нож        │  ← текст статов
│  └──────┘  Броня: Кожанка     │
│                               │
│  Склад: 12 стаков · 45.3 кг   │  ← createSubtitle
│                               │
│  ┌─────────────────────────┐  │
│  │      В ВЫЛАЗКУ          │  │  ← createButton, accent=true
│  └─────────────────────────┘  │
│  ┌──────────┐ ┌───────────┐   │
│  │Мастерская│ │ Инвентарь │   │  ← 2 кнопки в ряд (small 150px)
│  └──────────┘ └───────────┘   │
│  ┌──────────┐ ┌───────────┐   │
│  │  Радио   │ │Прогрессия │   │  ← 2 кнопки в ряд (small 150px)
│  └──────────┘ └───────────┘   │
│  [SFX ON] [Vol 75%]           │  ← текст справа сверху
│      [sticky banner]           │  ← баннер внизу
└──────────────────────────────┘
```

- Фон: `bg_forest` alpha 0.1, setDepth(-1)
- Кнопка «В вылазку» — accent (золотая #c5a267), остальные — стандартные
- Hero спрайт — из assets/sprites/hero.png
- SFX/Vol — интерактивный текст, меняется по клику (уже есть)
- Баннер — через showBanner() (уже есть)
- Шрифт заголовка: Impact, 28px

**Изменения в коде:**
- Заменить createButton вызовы на 1 акцентную + 4 small
- Добавить hero-спрайт и HP-бар (createHpBar)
- Добавить фон

---

## 2. CombatScene.ts — бой

Файл: `src/scenes/CombatScene.ts`

**Layout:**

```
┌──────────────────────────────┐
│           БОЙ                 │  ← createTitle
│     Босс: X  ·  Фаза 2       │  ← только если босс
│                               │
│  ┌──────┐  HP ████████░░      │  ← hero спрайт (x=40, scale 0.5)
│  │ hero │  Вес 5.0/30 кг     │  ← HP-бар (createHpBar, 200×8)
│  └──────┘                     │
│                               │
│  ┌────────────────────────┐  │
│  │ Мародёр  ██████░░  80   │  │  ← enemy панель (createPanel 320×130)
│  │ Пёс      ████░░░░  40   │  │  ← имя + HP-бар (createHpBar 160×6)
│  │ Мутант   повержен        │  │
│  └────────────────────────┘  │
│                      ┌──┐┌──┐ │  ← моб-спрайты (scale 0.35)
│                      └──┘└──┘ │
│                               │
│  > Герой бьёт Мародёра на 12  │  ← лог (4 строки, #8a8070, 12px)
│                               │
│  ┌────┐ ┌────┐ ┌────┐         │
│  │Удар│ │Защ │ │Леч │         │  ← smallButtons (90×36)
│  └────┘ └────┘ └────┘         │
│  ┌──────────────────────┐     │
│  │     ОТСТУПЛЕНИЕ       │     │  ← createButton (220×48)
│  └──────────────────────┘     │
└──────────────────────────────┘
```

**Изменения в коде:**
- `create()`: заменить 4 createButton на 3 createSmallButton + 1 createButton. Заменить createSubtitle на createPanel + createHpBar
- `updateDisplay()`: чистить старые HP-бары по data-тегу `hpBar`, рисовать новые createHpBar для hero + каждого живого моба. Моб-спрайты рендерить как есть (scale 0.35)
- Добавить `createHpBar`, `createSmallButton` в импорты
- Hero спрайт уже есть (из M9) — оставить

---

## 3. MapScene.ts — карта зон

Файл: `src/scenes/MapScene.ts`

**Layout (каждая зона — карточка 300×120):**

```
┌──────────────────────────────┐
│           КАРТА              │
│                               │
│  ┌─────────────────────────┐  │
│  │ ЛЕС (ур. 1)             │  │  ← название жирным 14px
│  │ Тёмный лес на окраине   │  │  ← описание мелким 12px
│  │              ┌────────┐ │  │
│  │              │ ВОЙТИ  │ │  │  ← createButton
│  │              └────────┘ │  │
│  └─────────────────────────┘  │
│                               │
│  ┌─────────────────────────┐  │
│  │ СКЛАД (ур. 3) 🔒        │  │  ← 🔒 если закрыто
│  │ Откроется после: Лес ур.2│  │
│  │              ┌────────┐ │  │
│  │              │ЗАКРЫТО │ │  │  ← alpha 0.5
│  │              └────────┘ │  │
│  └─────────────────────────┘  │
│                               │
│  ┌─────────────────────────┐  │
│  │ ГОРОД (ур. 5)           │  │
│  │ Руины делового центра   │  │
│  │  ┌────────┐ ┌─────────┐ │  │
│  │  │ ДЕЙЛИ  │ │ СБРОС▶  │ │  │  ← daily кнопка + реклама
│  │  └────────┘ └─────────┘ │  │
│  └─────────────────────────┘  │
│                               │
│  [Назад в Оплот]              │
└──────────────────────────────┘
```

**Изменения в коде:**
- Добавить фон `bg_forest` alpha 0.15
- Заменить текстовое описание на структурированную карточку (createPanel + createTitle-smaller + createSubtitle + createButton)
- Для закрытых зон: добавить 🔒 символ в название, кнопку затемнить alpha 0.5
- Daily-кнопку оставить как есть + кнопка сброса (уже есть)
- Опционально: камера скроллится если зон больше 4 (Phaser scrollFactor)

---

## 4. InventoryScene.ts — инвентарь

Файл: `src/scenes/InventoryScene.ts`

**Самый проблемный экран. Текущий: стена текста. Нужно: сетка иконок.**

**Layout:**

```
┌──────────────────────────────┐
│         ИНВЕНТАРЬ            │
│                               │
│  Склад: ██████████ 45.3/50 кг │  ← weight прогресс-бар
│                               │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐   │
│  │🪵│ │🔩│ │🧵│ │🍖│ │💧│   │  ← сетка иконок 48×48
│  │x3│ │x2│ │x5│ │x1│ │x4│   │  ← подпись количества
│  └──┘ └──┘ └──┘ └──┘ └──┘   │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐   │
│  │💣│ │🔫│ │🛡│ │⛑│ │💊│   │
│  │x1│ │x1│ │x1│ │x2│ │x3│   │
│  └──┘ └──┘ └──┘ └──┘ └──┘   │
│                               │
│  ┌──────┐  Оружие: Нож       │  ← иконка 64px + название
│  │ 🔪   │  ┌──────────┐     │
│  │      │  │ СМЕНИТЬ   │     │
│  └──────┘  └──────────┘     │
│  ┌──────┐  Броня: Кожанка    │
│  │ 🧥   │  ┌──────────┐     │
│  │      │  │ СМЕНИТЬ   │     │
│  └──────┘  └──────────┘     │
│                               │
│  [Назад]                      │
└──────────────────────────────┘
```

**Изменения в коде:**
- Убрать текстовый список stash, заменить сеткой иконок (Phaser images 48×48 в ряд по 5, расстояние 10px, позиции: x=40+idx%5*58, y=150+floor(idx/5)*58)
- Каждая иконка: `this.add.image(x, y, \`item_${id}\`).setScale(0.75)`
- Под каждой иконкой: `this.add.text(x, y+28, \`x${count}\`, ...).setOrigin(0.5)`
- Интерактивность: POINTER_OVER → tooltip с названием + весом (уже есть tooltip-логика)
- Weight прогресс-бар: createHpBar с другим цветом (#4682B4) для веса
- Экипировка: иконки 64×64 слева, название + кнопка смены справа (createSmallButton)
- Импорт: createHpBar, createSmallButton

---

## 5. CraftScene.ts — мастерская

Файл: `src/scenes/CraftScene.ts`

**Layout (каждый рецепт — карточка):**

```
┌──────────────────────────────┐
│        МАСТЕРСКАЯ            │
│  ░░░░░░░░░░ Создано: Нож ░░░░│  ← toast (если lastCrafted)
│                               │
│  ┌─────────────────────────┐  │
│  │ 🔪  ←  🪵×2 + 🔩×1     │  │  ← результат ← ингредиенты
│  │ Нож                    │  │
│  │              ┌────────┐ │  │
│  │              │СОЗДАТЬ │ │  │  ← зелёная если canCraft, серая если нет
│  │              └────────┘ │  │
│  └─────────────────────────┘  │
│                               │
│  ┌─────────────────────────┐  │
│  │ 🧥  ←  🧵×3 + 🔩×2     │  │
│  │ Кожанка                 │  │
│  │              ┌────────┐ │  │
│  │              │НЕ ХВАТ. │ │  │  ← + tooltip с недостающими
│  │              └────────┘ │  │
│  └─────────────────────────┘  │
│                               │
│  [Назад]                      │
└──────────────────────────────┘
```

**Изменения в коде:**
- Заменить текстовый список рецептов на карточки: createPanel 300×70 для каждого
- В каждой карточке: иконка результата (scale 0.6 слева), название, иконки ингредиентов с кол-вом (scale 0.4 каждый, в ряд), кнопка «Создать» / «Не хватает» справа (createSmallButton, зелёная/серая)
- Toast «Создано: X» — зелёный текст + твин всплытия (runTween)
- `canCraft()` / `formatMissing()` — оставить как есть, использовать для цвета кнопки

---

## 6. LootScene.ts — лут

Файл: `src/scenes/LootScene.ts`

**Layout:**

```
┌──────────────────────────────┐
│           ДОБЫЧА             │
│                               │
│  ┌──┐ ┌──┐ ┌──┐             │
│  │🪵│ │🔩│ │🧵│  ...        │  ← иконки предметов 48×48 в ряд
│  │x2│ │x1│ │x3│             │
│  └──┘ └──┘ └──┘             │
│                               │
│  ┌──────────────────────────┐│
│  │       ВЗЯТЬ ВСЁ          ││  ← accent кнопка
│  └──────────────────────────┘│
│  ┌──────────────────────────┐│
│  │   ВЕРНУТЬСЯ НА БАЗУ      ││  ← стандартная кнопка
│  └──────────────────────────┘│
└──────────────────────────────┘
```

**Изменения:**
- Заменить текстовый список loot на иконки (как в InventoryScene, но без интерактивности)
- Кнопки: accent + стандартная

---

## 7. ReturnScene.ts — возврат

Файл: `src/scenes/ReturnScene.ts`

**Изменения:**
- Заменить текст «Время возврата: Xс» на прогресс-бар (createHpBar с зелёным цветом #4682B4 как «возврат»)
- Hero-спрайт + анимация ходьбы (уже есть — runTween tween_return_walk)
- Кнопки ×2 лут / Вернуться — оставить как есть (уже M8b)
- Добавить interstitial после «Вернуться» — уже есть

---

## 8. LevelUpScene.ts — новый уровень

Файл: `src/scenes/LevelUpScene.ts`

**Layout: карточки перков**

```
┌──────────────────────────────┐
│       НОВЫЙ УРОВЕНЬ!         │
│          Уровень 5           │
│                               │
│  ┌──┐ Быстрый сборщик        │
│  │🧤│ +25% скорость сбора    │  ← иконка перка 64×64 + описание
│  └──┘    ┌────────┐         │
│           │ВЫБРАТЬ │         │  ← createSmallButton
│           └────────┘         │
│                               │
│  ┌──┐ Острый клинок          │
│  │🗡│ +15% урон в ближнем    │
│  └──┘    ┌────────┐         │
│           │ВЫБРАТЬ │         │
│           └────────┘         │
└──────────────────────────────┘
```

**Изменения:**
- Иконки перков из assets/sprites/perks/perk_*.png (уже есть, 8 шт)
- createPanel для каждого перка + иконка + createSmallButton «Выбрать»
- Количество перков: PERKS_PER_LEVEL_UP (из balance)

---

## 9. RadioScene.ts — радио

Файл: `src/scenes/RadioScene.ts`

**Изменения:**
- Иконки типа сигнала из assets/sprites/radio/radio_{truth,trap,ambiguous}.png (уже есть) — показывать рядом с сигналом
- Оставить логику решений без изменений (trust/clamp/already-resolved)

---

## 10. SortieScene.ts — вылазка

Файл: `src/scenes/SortieScene.ts`

**Изменения:**
- Заменить кнопки глубины на цветовую индикацию:
  - Depth 1: синяя (безопасно)
  - Depth 2: оранжевая (средне)
  - Depth 3: красная (опасно)
  - Если уровень персонажа недостаточен: серая + текст «Нужен ур. X»
- Фон: `bg_{zone_id}` alpha 0.2 (если есть)

---

## 11. BootScene.ts — загрузка

**Изменения: БЕЗ изменений.** Загрузочный экран уже функционален.

---

## Проверка перед PR

- [ ] `npm run typecheck` — чисто
- [ ] `npm run lint` — чисто
- [ ] `npm run test` — 213/213 PASS (без изменений в тестах)
- [ ] `npm run build` — JS ≤ 2 MB, assets ≤ 730 KB
- [ ] Визуально: все сцены читаемы, кнопки реагируют на hover/touch, HP-бары анимируются
- [ ] Mobile: кнопки ≥ 48×48 tap target, safe-area работает
- [ ] Anti-scope: игровая логика не тронута, JSON контент не изменён

---

## Что НЕ делается в этом PR

- Новые механики
- Баланс
- Анимации (кроме уже существующих tweens)
- Музыка / голос
- Новые спрайты (используем существующие из assets/)
- Шрифты Oswald/Roboto Mono (оставляем Impact/Arial — подключать шрифты отдельной задачей)
