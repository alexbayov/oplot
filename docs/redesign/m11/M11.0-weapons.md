# M11.0 — Weapon System Overhaul (детальный план)

> Версия: v2 (5 открытых вопросов закрыты Alex)
> Автор: Zo · PM: Alex
> Дата: 2026-05-28
> Родительский план: [`../M11-CORE-LOOP.md`](../M11-CORE-LOOP.md)
> Финальные решения по открытым вопросам: [`M11.0-questions-resolved.md`](M11.0-questions-resolved.md)

---

## TL;DR

Переходим от 80 «уровневых» предметов к **трёхклассовой системе:**

- 🔨 **Самодел** — крафтится из лома, ломкий, дешёвый, **высокий пиковый урон**, низкая надёжность
- 🎯 **Дроп** — реальное оружие СНГ-эпохи, **собирается из частей** добытых с мобов, надёжное, требует охоты
- 🔧 **Мод** — крафтится отдельно, навешивается на дроп, **меняет характеристики с трейдоффом**

**Принцип:** дроп **никогда не падает целиком**. Только детали (15–25% шанс) → собираешь на верстаке.

Все цифры в этом доке — **черновые**, балансируются в QA pass в конце фазы.

---

## 1. Item Model (новые поля)

Существующая модель `Item` расширяется. Старые поля сохраняются, новые добавляются через `ItemRegistry` адаптер (см. §11).

```ts
type ItemClass = "craft" | "drop" | "mod" | "part" | "resource" | "ammo" | "consumable" | "broken_craft";
type Caliber = "9x18" | "7.62x25" | "5.45x39" | "7.62x39" | "7.62x54R" | ".308" | "12ga" | "melee" | "thrown";

interface Item {
  // существующие поля
  id: string;
  name_ru: string;
  type: "weapon_melee" | "weapon_ranged" | "armor" | "resource" | "consumable";
  weight: number;
  // ...

  // НОВЫЕ поля (опциональны, ItemRegistry проставляет дефолты)
  itemClass?: ItemClass;            // дефолт: вывести из type
  tier?: 1 | 2 | 3 | 4 | 5;     // дефолт: 1
  caliber?: Caliber;            // только для weapon_ranged и ammo
  capacity?: number;            // ёмкость магазина (только weapon_ranged)
  rate_of_fire?: "single" | "semi" | "burst" | "auto";
  noise?: "silent" | "quiet" | "loud" | "very_loud";
  parts?: string[];             // ID деталей для сборки (только class=drop)
  mod_slots?: number;           // кол-во слотов под модификации (только class=drop)
  durability_max?: number;      // только class=craft (сколько ударов/выстрелов до поломки)
  attached_mods?: string[];     // runtime: какие моды навешены (хранится в save, не в каталоге)
  disassembleRecipe?: RecipeIngredient[];
}
```

### Самодел при поломке → `broken_craft`

**Решение Alex (Q4 в `M11.0-questions-resolved.md`).** Когда `durability_current = 0` у самодела:

1. Оружие **не пропадает**, превращается в `broken_<original_id>` (новый item класса `broken_craft`)
2. Сломанный самодел **не экипируется**, только лежит в инвентаре
3. На верстаке появляется action **«Разобрать сломанное»** (см. §8)
4. Разбор → ~30% от исходного рецепта (округление вниз), генерируется автоматически

**Пример:** «Поджига» (рецепт: 3 scrap + 2 wood + 1 gunpowder, всего 6 единиц).
При поломке → `broken_makeshift_pistol` (вес 0.6 кг, value 0).
Разбор на верстаке → 1 scrap + 1 wood (30% × 6 ≈ 2 единицы, выбираем самые ценные).

**Игровая роль:**
- Recycling loop: «сломал → таскай или выкинь → разбери → крафтани новый»
- Верстак нужен чаще
- Решение «таскать или выкинуть» добавляет вес-как-жадность

---

## 2. 🔨 Каталог самоделов (Craft)

**Принцип:** доступны с первого уровня, крафтятся из базовых ресурсов, **ломаются** через X использований. Высокий риск-реворд: бьют сильно, но мажут и разваливаются.

| ID | Название | Тир | Калибр | Урон | Точность | Шум | Прочность | Крафт |
|---|---|---|---|---|---|---|---|---|
| `craft_shiv` | Заточка | 1 | melee | 4 | 70% | silent | 30 уд | 2 лом |
| `craft_pipe_pistol` | Поджига | 2 | 9×18 | 6 | 55% | loud | 20 выстр | 5 лом + 2 дерево + 3 порох |
| `craft_pipe_rifle` | «Трубка» (винтовка) | 2 | 7.62×39 | 9 | 50% | very_loud | 15 выстр | 8 лом + 3 дерево + 4 порох |
| `craft_sawed_off` | Обрез | 2 | 12ga | 11 (AOE 2) | 60% | very_loud | 25 выстр | 6 лом + 2 дерево |
| `craft_spear` | Копьё | 1 | melee | 6 | 65% | silent | 40 уд | 1 лом + 4 дерево |
| `craft_crossbow` | Арбалет | 2 | thrown | 7 | 75% | silent | 60 выстр | 4 лом + 3 дерево + 2 верёвка |
| `craft_grenade` | Граната «Хлопушка» | 2 | thrown | 8 (AOE 3) | бросок | very_loud | 1 раз | 3 лом + 4 порох + 1 ткань |
| `craft_molotov` | Молотов | 2 | thrown | 5 + поджог | бросок | loud | 1 раз | 1 ткань + 1 масло |

**Сравнение с дропом (пример пистолетов):**

| Характеристика | 🔨 Поджига | 🎯 ПМ | 🎯 ТТ |
|---|---|---|---|
| Урон | 6 | 5 | 7 |
| Точность | 55% | 80% | 75% |
| Шум | loud | loud | loud |
| Прочность | **20 выстрелов** | надёжный | надёжный |
| Доступность | **крафт T2** | сборка из 3 частей | сборка из 3 частей |
| Калибр | 9×18 (общий) | 9×18 | 7.62×25 |

→ **Поджига сильнее ПМ по урону, но разваливается. ПМ ниже урон — но всегда работает.** Это и есть выбор, а не «плохое vs хорошее».

---

## 3. 🎯 Каталог дропа (реальные стволы)

### 3.1 Пистолеты (T2–T3)

| ID | Название | Тир | Калибр | Урон | Точность | Ёмкость | Mod slots | Части |
|---|---|---|---|---|---|---|---|---|
| `pm` | ПМ (Макаров) | 2 | 9×18 | 5 | 80% | 8 | 1 | рамка, затвор, магазин |
| `tt` | ТТ (Токарев) | 2 | 7.62×25 | 7 | 75% | 8 | 1 | рамка, затвор, магазин |
| `aps` | Стечкин АПС | 3 | 9×18 | 6 | 70% | 20 | 2 | рамка, затвор, магазин, кожух |

### 3.2 ПП и карабины (T2–T3)

| ID | Название | Тир | Калибр | Урон | Точность | Ёмкость | Mod slots | Части |
|---|---|---|---|---|---|---|---|---|
| `ppsh` | ППШ-41 | 3 | 7.62×25 | 6 (burst×3) | 65% | 35 | 1 | ствол, ствольная коробка, барабан, приклад |
| `sks` | СКС | 3 | 7.62×39 | 8 | 75% | 10 | 2 | ствол, затвор, магазин, приклад |
| `aks_74u` | АКС-74У | 3 | 5.45×39 | 7 (burst×3) | 70% | 30 | 2 | ствол, затворная рама, магазин, приклад |

### 3.3 Автоматы (T3–T4)

| ID | Название | Тир | Калибр | Урон | Точность | Ёмкость | Mod slots | Части |
|---|---|---|---|---|---|---|---|---|
| `akm` | АКМ | 3 | 7.62×39 | 9 (burst×3) | 70% | 30 | 2 | ствол, ствольная коробка, затворная рама, магазин, приклад |
| `ak_74` | АК-74 | 4 | 5.45×39 | 8 (burst×4) | 75% | 30 | 3 | ствол, ствольная коробка, затворная рама, магазин, приклад |
| `rpk` | РПК | 4 | 5.45×39 | 9 (burst×5) | 65% | 45 | 2 | ствол, ствольная коробка, затворная рама, барабан, сошки |

### 3.4 Снайперки (T4–T5)

| ID | Название (реальное) | Тир | Калибр | Урон | Особенность |
|---|---|---|---|---|---|
| `rifle_t3_hunting` | Охотничий карабин (.308) | 3 | .308 | 12 | Гражданское, 4 патрона, перезарядка медленная |
| `rifle_t4_mosin` | Мосинка (винтовка обр. 1891) | 4 | 7.62×54R | 14 | Болт, 5 патронов, тихий ход, без оптики |
| `rifle_t5_svd` | СВД | 5 | 7.62×54R | 18 | Полуавтомат, 10 патронов, +25% крит на дальней дистанции |

`rifle_t3_hunting` решает orphan-калибр `.308` (B6). Кроме него .308 нигде не используется.

### 3.5 Дробовики (T3–T4)

| ID | Название | Тир | Калибр | Урон | Точность | Ёмкость | Mod slots | Части |
|---|---|---|---|---|---|---|---|---|
| `iz_43` | ИЖ-43 (вертикалка) | 3 | 12ga | 12 (AOE 2) | 70% | 2 | 1 | стволы, цевьё, приклад |
| `saiga_12` | Сайга-12 | 4 | 12ga | 11 (AOE 3) | 65% | 8 | 2 | ствол, ствольная коробка, магазин, приклад |
| `bekas` | Бекас | 3 | 12ga | 13 (AOE 2) | 75% | 4 | 1 | ствол, цевьё, приклад |

### 3.6 Холодное оружие (T2–T3, дроп)

| ID | Название | Тир | Урон | Особенность | Источник |
|---|---|---|---|---|---|
| `nr_43` | Нож разведчика НР-43 «Вишня» | 2 | 5 | +10% крит | Капитан охраны, спецы |
| `combat_machete` | Армейский мачете | 3 | 7 | +AOE 2 | Городской мутант, босс |
| `kukri` | Кукри | 3 | 8 | +15% крит на melee | Редкий дроп с офицеров |

### 3.7 Гранаты (T3–T4, дроп)

| ID | Название | Тир | Урон | Особенность |
|---|---|---|---|---|
| `f1` | Граната Ф-1 | 3 | 14 (AOE 4) | Без осечки (vs «Хлопушка» 15%) |
| `rgd5` | Граната РГД-5 | 3 | 10 (AOE 3) | Меньше осколков, безопаснее |
| `rgo` | Граната РГО | 4 | 12 (AOE 3) | Мгновенный подрыв при контакте |

**Итого дропа: 22 единицы оружия** (3 пистолета + 3 ПП/карабина + 3 автомата + 2 снайперки + 3 дробовика + 3 ножа + 3 гранаты + 2 буфер).

---

## 4. Детали оружия (parts)

Каждая деталь — отдельный предмет в `items.json` с `class: "part"`. Группируется в инвентаре в раздел «Запчасти».

**Пример naming convention:**

```
pm_frame         — Рамка ПМ
pm_slide         — Затвор ПМ
pm_magazine      — Магазин ПМ (8 патронов 9×18)

ak74_barrel      — Ствол АК-74
ak74_receiver    — Ствольная коробка АК-74
ak74_bolt        — Затворная рама АК-74
ak74_magazine    — Магазин АК-74 (30 патронов 5.45×39)
ak74_stock       — Приклад АК-74
```

**Сколько частей на ствол:**

| Тир ствола | Частей |
|---|---|
| T2 пистолеты | 3 |
| T3 ПП/карабины/АКМ | 4 |
| T4 автоматы/снайперки/дробовики | 5 |
| T5 СВД, уникальные | 5 |

**Итого деталей** (грубо): 14 сборных стволов × среднее 4 = **~56 уникальных part-предметов**.

**Часть = одноразовая.** Собрал ствол — части потрачены. Если разобрать (M11.0+) — вернётся часть деталей с шансом.

### 4.1 Каталог partов и их источники

Источники: `mob_id` = конкретный моб; `zone scavenge` = общий лут зоны (контейнеры, тайники); `boss TBD M11.3` = редкая деталь, гарантированно с босса соответствующей Зачистки.

| Ствол | Тир | Части | Источники |
|---|---|---|---|
| **ПМ** | 2 | `pm_frame`, `pm_slide`, `pm_magazine` | marauder (8–15%), zone scavenge: forest/suburbs |
| **ТТ** | 2 | `tt_frame`, `tt_slide`, `tt_magazine` | marauder city (12%), fanatic_berserker (8%) |
| **АПС (Стечкин)** | 3 | `aps_frame`, `aps_slide`, `aps_magazine`, `aps_stock` | city_guard_captain (15%), zone scavenge: city |
| **ППШ** | 3 | `ppsh_receiver`, `ppsh_barrel`, `ppsh_drum`, `ppsh_stock` | fanatic_berserker (5–10%), zone scavenge: city |
| **СКС** | 3 | `sks_receiver`, `sks_barrel`, `sks_bolt`, `sks_stock` | armored_guard (10%), looter_sniper (8%), zone scavenge: warehouse |
| **АКС-74У** | 3 | `aks74u_barrel`, `aks74u_receiver`, `aks74u_bolt`, `aks74u_magazine` | armored_guard (10–18%), zone scavenge: warehouse |
| **АКМ** | 3 | `akm_barrel`, `akm_receiver`, `akm_bolt`, `akm_magazine` | fanatic_berserker (8%), city_guard_captain (10%), zone scavenge: city |
| **АК-74** | 4 | `ak74_barrel`, `ak74_receiver`, `ak74_bolt`, `ak74_magazine`, `ak74_stock` | armored_guard (15–20%), city_guard_captain (15–18%), **boss factory M11.3 (guaranteed)** |
| **РПК** | 4 | `rpk_barrel`, `rpk_receiver`, `rpk_bolt`, `rpk_magazine`, `rpk_bipod` | city_guard_captain (10%), warehouse_drone_prime (50% barrel), **boss factory M11.3** |
| **Охот. карабин (.308)** | 3 | `hunting_receiver`, `hunting_barrel`, `hunting_bolt`, `hunting_scope` | marauder forest (rare 5%), zone scavenge: forest/suburbs |
| **Мосина** | 4 | `mosin_receiver`, `mosin_barrel`, `mosin_bolt`, `mosin_stock` | looter_sniper (12%), zone scavenge: warehouse/city |
| **СВД** | 5 | `svd_barrel`, `svd_receiver`, `svd_bolt`, `svd_magazine`, `svd_stock` | looter_sniper (10–15%), **boss metro M11.3 barrel (guaranteed)** |
| **ИЖ-43** | 3 | `izh43_receiver`, `izh43_barrels`, `izh43_stock` | marauder forest (8%), zone scavenge: forest/suburbs |
| **Сайга-12** | 4 | `saiga_barrel`, `saiga_receiver`, `saiga_bolt`, `saiga_magazine` | fanatic_berserker (rare 5%), looter_sniper (8%) |
| **Бекас** | 3 | `bekas_receiver`, `bekas_barrel`, `bekas_pump`, `bekas_stock` | marauder city (5%), fanatic_berserker (8%) |

**Принцип покрытия:**
- **Все 14 стволов** имеют хотя бы один источник на существующих мобах
- **T5 финальные парты** (`ak74_barrel`, `rpk_barrel`, `svd_barrel`) — гарантированно с боссов M11.3 (вне зависимости от рандома мобов)
- **Zone scavenge** = общий лут зоны (контейнеры, тайники в `EncounterScene`), не привязан к мобам — это страховка от «не повезло с дропами»

`forest_alpha_mutant` (альфа леса) и `warehouse_drone_prime` (прайм-дрон) — открытые мини-боссы, не Зачистки. Они уже прописаны в §7.3.

---

## 5. 🔧 Каталог модификаций

| ID | Название | Тир | Подходит к | Эффект |
|---|---|---|---|---|
| `mod_pbs1` | Глушитель ПБС-1 | 3 | АКМ, АКС-74У, АК-74 | -1 урон, шум: loud → quiet |
| `mod_pbs_universal` | Глушитель универсал | 2 | пистолеты, ППШ | -1 урон, шум: loud → quiet |
| `mod_pso1` | Оптика ПСО-1 | 4 | СВД, АКМ, АК-74 | +20% точность с дистанции, заготовка под прицельный M12 |
| `mod_optic_4x` | Оптика 4× | 3 | СКС, Мосина, охотничьи | +15% точность, -1 ёмкость (мешает быстро перезарядить) |
| `mod_ext_mag_9x18` | Расширенный магазин 9×18 | 3 | ПМ, АПС | +50% ёмкость |
| `mod_ext_mag_545` | Расширенный магазин 5.45 | 4 | АКС-74У, АК-74, РПК | +50% ёмкость |
| `mod_tac_grip` | Тактическая рукоять | 3 | АКМ, АК-74, Сайга-12 | +10% точность в burst-режиме |
| `mod_bayonet` | Штык-нож 6×4 | 3 | АКМ, АК-74 | +2 урона в melee, занимает 1 mod-slot |

**Логика трейдоффов:** мод **не плоский бонус**. Глушитель = тише, но слабее. Оптика = точнее, но медленнее. Это решения, а не апгрейды.

**Крафт модов:** все моды крафтятся на верстаке (T2 рецепты, требуют 1 деталь + ресурсы). Не падают с мобов (кроме ПСО-1 — редкий лут с офицеров).

---

## 6. Боеприпасы (ammo)

Калибры — **отдельные стекаемые ресурсы** в инвентаре, расходуются при выстреле.

| ID | Калибр | К какому оружию | Где находить |
|---|---|---|---|
| `ammo_9x18` | 9×18 (ПМ) | ПМ, АПС, Поджига | Мародёры, ранние мобы |
| `ammo_762x25` | 7.62×25 (ТТ) | ТТ, ППШ | Городские бандиты, ППШ-рейдеры |
| `ammo_545` | 5.45×39 | АКС-74У, АК-74, РПК | Армейские мобы, капитаны |
| `ammo_762x39` | 7.62×39 | СКС, АКМ, «Трубка» | Сталкеры, охотники, ранние армейцы |
| `ammo_762x54r` | 7.62×54R | Мосина, СВД | Снайперы, военные офицеры |
| `ammo_12ga` | 12 калибр | ИЖ-43, Сайга-12, Бекас, Обрез | Охотники, выживальщики |
| `ammo_308` | .308 Winchester | Охотничья винтовка | Охотники-выживальщики (резерв) |

**Принцип:** редкий калибр = мощное оружие, но патроны дефицит. Хочешь играть СВД — будь готов экономить или ходить за патронами в специальные зоны.

---

## 7. Drop Tables на мобах

> **Важно (для QA):** новый формат `content/mobs.json` появляется в **M11.0c** (Drop Tables phase). В M11.0a/b мобы остаются как есть. Mob IDs ниже — **только реальные из текущего `content/mobs.json` (11 мобов)**. Босс-мобы для «Зачисток» (`factory_brigadier`, `tunnel_lord`, `forest_apex`) будут заведены в M11.3, до этого помечены TBD.

Каждый моб получает поле `drops` со списком возможных предметов. Шансы независимы (могут выпасть все).

### 7.1 Базовые мобы (T1–T2)

```yaml
marauder:                  # лес + город, базовый враг
  drops:
    - { id: "scrap_metal",   chance: 0.80, count: [1, 3] }
    - { id: "cloth",         chance: 0.40, count: [1, 2] }
    - { id: "ammo_9x18",     chance: 0.30, count: [3, 8] }
    - { id: "pm_magazine",   chance: 0.08 }
    - { id: "craft_shiv",    chance: 0.10 }   # уже собранная заточка
    - { id: "ammo_762x25",   chance: 0.15, count: [3, 6] }   # для мародёров в городе

wild_dog:
  drops:
    - { id: "leather",       chance: 0.85, count: [1, 2] }
    - { id: "raw_meat",      chance: 0.60, count: [1, 2] }
    - { id: "bone",          chance: 0.40, count: [1, 2] }

mutant:
  drops:
    - { id: "raw_meat",      chance: 0.70, count: [1, 3] }
    - { id: "mutated_gland", chance: 0.25 }
    - { id: "scrap_metal",   chance: 0.30, count: [1, 2] }

pack_rat:
  drops:
    - { id: "leather",       chance: 0.90, count: [1, 2] }
    - { id: "bone",          chance: 0.60, count: [1, 3] }
    - { id: "raw_meat",      chance: 0.40, count: [1, 2] }
```

### 7.2 Спец-мобы (T2–T4)

```yaml
fanatic_berserker:                   # городской вооружённый
  drops:
    - { id: "scrap_metal",   chance: 0.70, count: [2, 4] }
    - { id: "ammo_762x25",   chance: 0.35, count: [4, 10] }
    - { id: "tt_magazine",   chance: 0.12 }
    - { id: "tt_slide",      chance: 0.08 }
    - { id: "leather_jacket", chance: 0.05 }
    - { id: "ppsh_drum",     chance: 0.05 }   # ППШ-парт с фанатиков с автоматами

looter_sniper:                       # снайпер на складе
  drops:
    - { id: "ammo_762x54r",  chance: 0.70, count: [3, 8] }
    - { id: "svd_bolt",      chance: 0.10 }
    - { id: "svd_magazine",  chance: 0.15 }
    - { id: "mosin_bolt",    chance: 0.12 }
    - { id: "mod_pso1",      chance: 0.04 }      # редкий мод

armored_guard:                       # бронированный охранник
  drops:
    - { id: "ammo_545",      chance: 0.60, count: [8, 15] }
    - { id: "ak74_magazine", chance: 0.20 }
    - { id: "ak74_stock",    chance: 0.18 }
    - { id: "ak74_bolt",     chance: 0.15 }
    - { id: "aks74u_barrel", chance: 0.10 }
    - { id: "armored_vest_part", chance: 0.10 }

relic_drone:                         # реликтовый дрон
  drops:
    - { id: "electronics",   chance: 0.70, count: [1, 2] }
    - { id: "microchips",    chance: 0.40 }
    - { id: "drone_core",    chance: 0.15 }

city_guard_captain:                  # капитан охраны, мини-босс
  drops:
    - { id: "ammo_545",      chance: 0.80, count: [15, 30] }
    - { id: "ak74_receiver", chance: 0.18 }      # редкая деталь
    - { id: "ak74_barrel",   chance: 0.15 }      # редкая деталь
    - { id: "mod_pbs1",      chance: 0.05 }      # модификация (готовая)
    - { id: "nr_43",         chance: 0.08 }      # армейский нож НР-43
```

### 7.3 Альфа-мобы (T4, открытые боссы зон)

```yaml
forest_alpha_mutant:                 # лесной альфа
  drops:
    - { id: "raw_meat",          chance: 1.00, count: [3, 5] }
    - { id: "mutated_gland",     chance: 0.80 }
    - { id: "leather",           chance: 0.90, count: [2, 4] }
    - { id: "alpha_pelt",        chance: 0.30 }   # T4 крафт-материал

warehouse_drone_prime:               # прайм-дрон, мини-босс склада
  guaranteed_drops:
    - { id: "drone_core" }                 # 100%
    - { id: "rpk_barrel" }                 # 100%, ствол РПК
  random_drops:
    - { id: "mod_optic_pso1",    chance: 0.40 }
    - { id: "ak74_receiver",     chance: 0.50 }
```

### 7.4 Боссы M11.3 «Зачисток» (TBD)

> Финальный список боссов «Зачисток» появится в [`M11.3-dungeon-clear.md`](M11.3-dungeon-clear.md). Здесь — placeholder'ы для тех guaranteed-дропов, на которые ссылаются рецепты T5:

```yaml
boss_factory_brigadier:    # TBD: M11.3 Зачистка «Фабрика»
  guaranteed_drops:
    - { id: "ak74_receiver" }              # 100%
    - { id: "armored_vest_recipe" }        # 100%, чертёж
  random_drops:
    - { id: "rpk_barrel",        chance: 0.50 }
    - { id: "mod_optic_pso1",    chance: 0.30 }

boss_metro_warden:         # TBD: M11.3 Зачистка «Метро»
  guaranteed_drops:
    - { id: "svd_barrel" }                 # 100%
  random_drops:
    - { id: "mod_pbs1",          chance: 0.50 }
    - { id: "kukri",             chance: 0.20 }

boss_hospital_chief:       # TBD: M11.3 Зачистка «Госпиталь»
  guaranteed_drops:
    - { id: "rare_meds_recipe" }           # чертёж
  random_drops:
    - { id: "mod_collimator",    chance: 0.40 }
```

**Принцип:** редкие детали стволов T4–T5 (ствольная коробка АК, ствол СВД) — только с боссов M11.3. Это **закрепляет петлю «играть в Зачистки = собрать топовое»**.

---

## 8. Изменения в Workbench (CraftScene)

Сейчас Workbench умеет один режим: «крафт по рецепту». Добавляем два новых режима:

### 8.1 Режим «Собрать оружие» (новый)

UI:
- Список **дроп-стволов**, для которых есть хотя бы 1 деталь
- Для каждого — прогрессбар «3/5 деталей»
- Кнопка «Собрать» активна когда **все детали есть**
- При сборке детали тратятся, в инвентарь падает готовое оружие

### 8.2 Режим «Модификация» (новый, через инвентарь)

UI открывается из инвентаря на конкретном оружии:
- Видны mod_slots (квадратики)
- Можно перетащить совместимый мод из инвентаря в слот
- Tooltip оружия пересчитывается: «ТТ — 7 урона / loud» → «ТТ — 6 урона / quiet [ПБС]»
- Можно снять мод обратно (бесплатно)

### 8.3 Режим «Крафт» (существующий, остаётся)

Без изменений, но рецептов прибавляется:
- Все самоделы (8 шт)
- Все моды (8 шт)
- Базовые расходники

### 8.4 Режим «Разобрать сломанное» (новый)

**Триггер:** игрок выбирает в инвентаре сломанный самодел (`itemClass === "broken_craft"`) и жмёт «Разобрать на верстаке».

**Логика:**
1. `ItemRegistry.computeDisassembleYield(brokenItemId)` возвращает массив `RecipeIngredient[]`
2. Игрок видит превью: «получишь: 1× scrap, 1× wood»
3. Подтверждает → сломанный самодел удаляется, ресурсы зачисляются на склад

**Авто-генерация yield:**
- Берётся `recipe` исходного самодела
- Каждый ингредиент × 0.3, округление вниз
- Если все < 1, гарантируется минимум 1 единица самого ценного ресурса (чтобы разбор всегда что-то давал)

---

## 9. UI изменения

### 9.1 Инвентарь

- Новая вкладка **«Запчасти»** — отдельно от Оружия/Брони/Ресурсов
- В Запчастях детали **группируются по стволу**: «ПМ (3/3) — собрать» / «АК-74 (2/5)»
- На иконке оружия с навешенными модами — мелкий значок мода в углу
- Tooltip оружия показывает: класс, тир, калибр, урон, точность, шум, mod_slots, навешенные моды

### 9.2 UI лута после боя

- Детали оружия подсвечиваются **оранжевой рамкой** (это редкий лут)
- Моды подсвечиваются **фиолетовой рамкой**
- Над окном лута — счётчик прогресса: «АК-74: было 1/5 → стало 2/5»

### 9.3 Иконки

- Самоделы — в текущей painted-стилистике, грубые и угловатые
- Дроп-стволы — детальные, узнаваемые силуэты (ПМ ≠ ТТ визуально)
- Детали — каждая своя иконка, но в **сером тоне** (не готовое изделие)
- Моды — мелкие иконки в **синем тоне**

**Объём арт-работы:** ~60 новых иконок (8 самоделов + 22 ствола + ~25 деталей-укрупнённо + 8 модов). По 64×64, ~3 KB каждая = **~180 KB прибавки к билду**.

---

## 10. Calibre Compatibility Matrix

Полезно для балансировки: показывает какие калибры в какие стволы.

| Калибр | Самодел | Дроп |
|---|---|---|
| **9×18** | Поджига | ПМ, АПС |
| **7.62×25** | — | ТТ, ППШ |
| **5.45×39** | — | АКС-74У, АК-74, РПК |
| **7.62×39** | Трубка | СКС, АКМ |
| **7.62×54R** | — | Мосина, СВД |
| **12ga** | Обрез | ИЖ-43, Сайга-12, Бекас |
| **.308** | — | Охотничья винтовка |
| **melee** | Заточка, Копьё | НР-43, мачете, кукри, штык |
| **thrown** | Арбалет, Хлопушка, Молотов | Ф-1, РГД-5, РГО |

---

## 11. ItemRegistry API

Новый файл `src/state/ItemRegistry.ts`. Единая точка чтения предметов.

```ts
// src/state/ItemRegistry.ts
import itemsJson from "../../content/items.json";

class ItemRegistry {
  private items = new Map<string, Item>();

  constructor() {
    for (const raw of itemsJson) {
      this.items.set(raw.id, this.normalize(raw));
    }
  }

  get(id: string): Item | undefined {
    return this.items.get(id);
  }

  // Адаптер: старые предметы получают дефолтные значения новых полей
  private normalize(raw: any): Item {
    return {
      ...raw,
      class: raw.class ?? this.inferClass(raw),
      tier: raw.tier ?? 1,
      mod_slots: raw.mod_slots ?? 0,
      parts: raw.parts ?? [],
      attached_mods: [], // runtime поле, всегда пусто в каталоге
    };
  }

  private inferClass(raw: any): ItemClass {
    if (raw.type === "resource") return "resource";
    if (raw.type === "consumable") return "consumable";
    if (raw.type?.startsWith("weapon")) {
      // старые «выдуманные» оружия → класс craft, надо ребалансить вручную
      return "craft";
    }
    return "resource";
  }
}

export const itemRegistry = new ItemRegistry();
```

**Итог:** старые тесты, которые делают `items.find(i => i.id === "sawed_off")`, продолжают работать. Новые тесты пользуются `itemRegistry.get("sawed_off")` и видят все новые поля.

- `isBrokenCraft(itemId): boolean` — true если itemClass === "broken_craft"
- `computeDisassembleYield(brokenItemId): RecipeIngredient[]` — авто-yield 30% от оригинального рецепта
- `getBrokenVariant(craftItemId): string | null` — для самодела вернуть id сломанной версии (`makeshift_pistol → broken_makeshift_pistol`)

### 11.2 Migration Map v1 → v2 (B4)

Старые `items.json` ID → новые. Engineer **обязан** применить эту карту в `migrateSave(v1 → v2)`, иначе у игроков будут дубли в инвентаре (старый `knife` + новый `craft_knife`).

| OLD id (текущий items.json) | NEW id (M11.0a) | Тип миграции | Заметка |
|---|---|---|---|
| `scrap` | `scrap_metal` | rename | переименование в long form |
| `cloth` | `cloth` | unchanged | |
| `wood` | `wood` | unchanged | |
| `food` | `canned_food` | rename | |
| `water` | `water` | unchanged | |
| `leather` | `leather` | unchanged | |
| `oil` | `machine_oil` | rename | |
| `gunpowder` | `gunpowder` | unchanged | |
| `electronics` | `electronics` | unchanged | |
| `microchips` | `microchips` | unchanged | |
| `rope` | `rope` | unchanged | |
| `medkit` | `medkit` | unchanged | |
| `bandage` | `bandage` | unchanged | |
| `painkiller` | `painkiller` | unchanged | |
| `ammo` | `ammo_9x18` | **upgrade-and-split** | обобщённые патроны → 9×18 (самый ранний), остальные калибры в M11.0c |
| `knife` | `craft_knife` | rename + reclassify | itemClass="craft" |
| `shiv` | `craft_shiv` | rename + reclassify | itemClass="craft" (стек если у игрока было два knife+shiv → объединяем в craft_shiv?) — **см. ниже** |
| `machete` | `craft_machete` | rename + reclassify | |
| `sawed_off` | `craft_sawed_off` | rename + reclassify | itemClass="craft" |
| `hunting_rifle` | `rifle_t3_hunting` | rename + reclassify | itemClass="drop", становится сборным (M11.0c обновит) |
| `makeshift_pistol` | `craft_makeshift_pistol` | rename + reclassify | в M11.0-weapons.md называется «Поджига» |
| `pipe_rifle` | `craft_pipe_rifle` | rename + reclassify | «Трубка» |
| `composite_blade` | `craft_composite_blade` | rename + reclassify | |
| `kuvalda` | `craft_sledge` | rename + reclassify | |
| `crowbar` | `craft_crowbar` | rename + reclassify | |
| `crossbow` | `craft_crossbow` | rename + reclassify | |
| `spear` | `craft_spear` | rename + reclassify | |
| `flare_gun` | `craft_flare_gun` | rename + reclassify | «Хлопушка» |
| _(новые предметы из M11.0)_ | _(добавляются)_ | new | parts, mods, реальные стволы |

**Конфликтный случай knife + shiv:**
В исходных items.json `knife` и `shiv` — два разных предмета. Если у игрока в инвентаре оба, миграция **сохраняет оба отдельно**: `knife → craft_knife`, `shiv → craft_shiv`. Это два разных craft-предмета по характеристикам (заточка vs нож), не путаем.

**Алгоритм migrateSave(v1 → v2):**

```ts
function migrateSave(save: SaveV1): SaveV2 {
  const MIGRATION_MAP: Record<string, string> = {
    "scrap": "scrap_metal",
    "food": "canned_food",
    "oil": "machine_oil",
    "ammo": "ammo_9x18",
    "knife": "craft_knife",
    "shiv": "craft_shiv",
    "machete": "craft_machete",
    "sawed_off": "craft_sawed_off",
    "hunting_rifle": "rifle_t3_hunting",
    "makeshift_pistol": "craft_makeshift_pistol",
    "pipe_rifle": "craft_pipe_rifle",
    "composite_blade": "craft_composite_blade",
    "kuvalda": "craft_sledge",
    "crowbar": "craft_crowbar",
    "crossbow": "craft_crossbow",
    "spear": "craft_spear",
    "flare_gun": "craft_flare_gun",
  };
  // … применяется к save.inventory, save.equipped, save.storage
  return { ...save, version: 2, inventory: remap(save.inventory) };
}
```

**Тесты обязательные:**
- `migrateSave_old_ammo_becomes_9x18`
- `migrateSave_knife_and_shiv_both_preserved`
- `migrateSave_idempotent` (применил дважды — то же)
- `migrateSave_unknown_id_passes_through_with_warning`

---

## 12. Save Migration v1 → v2

В `GameState` добавляется функция миграции, вызывается при загрузке сейва.

```ts
// src/state/migrations.ts
export const SAVE_VERSION = 2;

export function migrateSave(save: any): GameState {
  if (!save.version) save.version = 1;

  if (save.version === 1) {
    save = migrateV1toV2(save);
  }

  return save as GameState;
}

function migrateV1toV2(save: any): any {
  // 1. Старые предметы остаются как есть, просто получают дефолтные новые поля через ItemRegistry
  // 2. Удалённые предметы (mutated_gland, prime_circuit) → конвертация:
  for (const stack of save.inventory ?? []) {
    if (stack.item_id === "mutated_gland") {
      stack.item_id = "ak74_bolt";  // легендарный дроп вместо устаревшего ресурса
    }
    if (stack.item_id === "prime_circuit") {
      stack.item_id = "mod_pso1";   // конвертация в редкую модификацию
    }
  }
  // 3. Установить версию
  save.version = 2;
  return save;
}
```

**Тест:** `tests/migration.test.ts` загружает «эталонный сейв v1» из фикстуры и проверяет что после миграции игра запускается, инвентарь не теряется, деньги/уровень/перки сохраняются.

---

## 13. Тестирование

**Новые тесты (vitest), минимум:**

- `items-model.test.ts` — ItemRegistry, нормализация полей
- `assembly.test.ts` — сборка ствола из частей, проверка прогресса
- `mods.test.ts` — навешивание/снятие модов, пересчёт характеристик
- `drop-tables.test.ts` — детерминизм дропа при фиксированном rng
- `migration.test.ts` — v1 сейв → v2, без потерь
- `crafting-recipes.test.ts` — все новые рецепты крафтятся
- `durability.test.ts` — самоделы ломаются после N использований

**Existing tests:** все 248 должны остаться зелёными после введения адаптера. Если какие-то ломаются — фиксируем фикстуры, не отключаем тесты.

---

## 14. Открытые вопросы (требуют решения до старта)

**✅ ALL 5 CLOSED — см. [`M11.0-questions-resolved.md`](M11.0-questions-resolved.md) v2 (FINAL).**

Сводка решений:
1. ✅ Реальные названия — generic ID + name_real_ru / name_generic_ru, переключатель в config.ts
2. ✅ Цена модов — двухуровневая (ресурсы + трофейные детали для уникальных)
3. ✅ Снятие мода — разрешено, шанс поломки мода 20%
4. ✏️ Самодел при поломке — НЕ scrap_metal, а **broken_craft** → разбор на верстаке (30%)
5. ✅ Лимит сборки одного типа — нет, только вес. Рынок в M13+

---

## 15. Связанные файлы (которые будут трогаться)

- `content/items.json` — мажорное расширение (с ~80 до ~200 предметов)
- `content/recipes.json` — мажорное расширение (с 42 до ~80 рецептов)
- `content/mobs.json` — добавление `drops` полей всем 11 мобам
- `src/state/ItemRegistry.ts` — **новый файл**
- `src/state/migrations.ts` — **новый файл**
- `src/state/GameState.ts` — вызов миграции при загрузке
- `src/systems/loot.ts` — переход на mob drop tables (вместо zone-based)
- `src/systems/crafting.ts` — поддержка `recipe_type: "assemble"`
- `src/scenes/CraftScene.ts` — новый режим «Собрать оружие»
- `src/scenes/InventoryScene.ts` — вкладка «Запчасти», UI модификаций
- `src/ui/LootOverlay.ts` — подсветка деталей и модов
- `assets/icons/items/` — ~60 новых painted иконок
- `tests/` — ~7 новых файлов тестов

---

## 16. Готовность к старту

**Чтобы стартовать M11.0a, нужно:**

- [x] Мастер-план M11 утверждён
- [x] Этот детальный план существует
- [ ] Открытые вопросы §14 закрыты с Alex
- [ ] Создан kickoff PR с веткой `m11/0a-items-model`
- [ ] Художник готов брать в работу первую партию иконок (8 самоделов + 3 пистолета как proof of style)

Когда все 5 пунктов 🟢 — стартуем.
