# Handoff: Engineer — M11.0b (Content Wire-up)

> Состояние: **DONE_PENDING_REVIEW**
> Автор: Zo (Engineer)
> Дата: 2026-05-28
> Зависит от: M11.0a/eng (#96) + M11.0a/content (#95)

---

## Контекст

После мерджа M11.0a:
- **Eng PR #96** дал типы и `ItemRegistry` (но registry был пустой, fallback на legacy)
- **Content PR #95** залил 187 items + 71 рецепт **в гибридном формате** (legacy `type` + M11 поля `item_class`, `caliber`, `mod_slot`, и т.д.)

Проблема: `ItemRegistry.adaptLegacyItem` понимал ТОЛЬКО старый legacy формат, игнорировал все M11 поля Devin'а. Получалось: content есть — Registry не видит.

M11.0b — **wire-up**: научить адаптер читать гибрид + автоматически загружать весь content при `setContent`.

---

## Что реализовано

### `src/state/ItemRegistry.ts` (+114 строк)

Расширен `adaptLegacyItem`: теперь читает Devin'овский гибрид