# Handoff: Engineer — M12.0b (Combat Wire-up)

> Состояние: **DONE_PENDING_REVIEW**
> Автор: Zo (Engineer)
> Дата: 2026-05-28
> Спека: `docs/redesign/m12/M12.0-foundation.md` §3 (M11 wire-up) + §4 (durability/reload UI ready)
> Branch: `m12.0b/eng-wireup` → `m12-integration`
> Зависит от: M12.0a (#107) ✅

## TL;DR

Skeleton M12.0a получил **поведение**. Mod effects, durability, reload, status/cooldown tick — всё работает. CombatScene всё ещё не тронута (M12.0c).

## Реализовано (5 фаз)

| Фаза | Что | Файлы |
|---|---|---|
| 1 | **Mod aggregation** — `aggregateModEffects(WeaponInstance)` → damage/accuracy/noise/mag delta | `src/systems/modEffects.ts` (new, 77 строк) |
| 2 | **Durability tick** — -1 per attack, при 0 → equipped=null + log "weapon_break" | `combatEngine.executeAttack` |
| 3 | **Reload action** — magazine ← magazineMax, 1 turn cost, лог "reload" | `combatEngine.resolveHeroAction` |
| 4 | **mobAI v1 wire** — `resolveMobActions` уже использует `chooseMobAction` (M12.0a) | — |
| 5 | **Status/cooldown tick** — `endTurn` дёргает оба tick'а | `combatEngine.endTurn` (M12.0a) |

## Скоуп

**Реализовано:**
- Modэффекты складываются: 3 мода ПБС+прицел+магазин → суммарный delta
- ПБС перебивает noise оружия (silent priority)
- Durability decrement, breakage triggered at 0
- Reload action — full magazine refill, 1 turn cost
- LogKind расширен: +`weapon_break`, +`reload`

**Anti-scope (соблюдён):**
- `CombatScene.ts` НЕ тронут (M12.0c)
- НЕТ статус-эффектов поведения (M12.1)
- НЕТ активных приёмов оружия (M12.1)
- НЕТ position/environment (M12.3)
- НЕТ phase transitions (M12.4)

## Метрики

| Check | Before | After |
|---|---|---|
| Tests | 310 | **321** (+11) |
| Typecheck | clean | clean |
| Lint | 6 pre-existing | 6 (0 моих) |
| Build | 1.57 MB | 1.57 MB |

## Новые файлы
- `src/systems/modEffects.ts` (77) — aggregateModEffects + AggregatedModEffects
- `src/systems/__tests__/modEffects.test.ts` (90) — 5 тестов

## Изменённые
- `src/systems/combatEngine.ts` — executeAttack: mods + durability, resolveHeroAction "reload" case
- `src/systems/combatTypes.ts` — LogKind +weapon_break +reload
- `src/systems/__tests__/combatEngine.test.ts` — +6 тестов

## Что дальше (M12.0c)
- Подменить `CombatScene` calls на `CombatEngine.resolveHeroAction`
- HP-бары мобов
- Targeting click
- Weapon tooltip с caliber/mods/noise

## Готов к QA-Accept

Если REWORK — блокеры применяются к `m12.0b/eng-wireup` напрямую.
