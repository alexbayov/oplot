# Kickoff: Engineer — Веха M11.0a (Weapons Foundation)

Ты — **Engineer** на M11.0a (Weapons Foundation) проекта «Оплот».

Repo: https://github.com/alexbayov/oplot
Brief: `docs/redesign/m11/M11.0-weapons.md` + `docs/redesign/m11/M11.0-questions-resolved.md`
Base branch: `m11-integration`

## When

Start only after:
- PR #93 (M11 plan) merged into main
- `m11-integration` branch создана из main
- Alex закрыл 5 открытых вопросов в `M11.0-questions-resolved.md`
- QA-Spec verdict APPROVE по `M11.0-weapons.md`

## Context

- M10 закрыта (Living Refuge), теперь M11 — Core Loop Overhaul
- M11.0a — **архитектурный фундамент**: модель данных + миграция + адаптер
- Никакого UI! UI идёт в M11.0d отдельным PR.
- Content Designer параллельно пишет `content/items.json` — твой код должен **уметь читать и старый, и новый формат**.
- Полная спека модели — в `M11.0-weapons.md` §1.

## Do this

1. Checkout `m11-integration`. Read:
   - `docs/redesign/M11-CORE-LOOP.md`
   - `docs/redesign/m11/M11.0-weapons.md` (целиком, особенно §1 Item Model и §11 ItemRegistry)
   - `docs/redesign/m11/M11.0-questions-resolved.md` (финальные решения)
   - `src/state/GameState.ts`, `src/types/`, `src/systems/loot.ts`, `src/systems/crafting.ts`
2. Baseline: `npm run typecheck`, `npm run lint`, `npm test` (текущее покрытие), `npm run build`.
3. Branch `m11.0a/eng`. Draft PR `m11.0a/eng → m11-integration`.
4. **Реализуй:**
   - `src/types/items.ts` — новые типы: `ItemClass`, `Caliber`, `WeaponInstance`, `Part`, `Mod`, `ModSlot`, расширенный `Item`
   - `src/state/ItemRegistry.ts` — **новый файл**:
     - `getItem(id: string): Item` — читает items.json, проставляет дефолты для новых полей
     - `itemName(item: Item): string` — учитывает `WEAPON_NAMING_MODE` из config.ts
     - `isWeaponInstance(x: unknown): x is WeaponInstance` — type guard
     - `createWeaponInstance(itemId: string, parts: Part[]): WeaponInstance` — сборка из партов
   - `src/state/migrations.ts` — **новый файл**:
     - `migrateSave(state: unknown): GameState` — поддержка save v1 → v2
     - Старые stack-based weapon items конвертируются в WeaponInstance с default mods
     - Старые resource items проходят без изменений
   - `src/config.ts` — добавь `WEAPON_NAMING_MODE: "real" | "generic" = "real"`
   - `src/state/GameState.ts` — вызов `migrateSave()` в `loadGame()`
   - `src/systems/durability.ts` — **новый файл**: `onWeaponBreak(weapon, inventory)` (по решению вопроса 4)
5. **НЕ трогай** loot.ts, crafting.ts, UI сцены — они в M11.0b–d.
6. **Тесты ~10 новых:**
   - `src/state/__tests__/ItemRegistry.test.ts` — getItem, itemName с двумя режимами, type guards
   - `src/state/__tests__/migrations.test.ts` — миграция v1 save → v2, edge cases (пустой save, частичный)
   - `src/systems/__tests__/durability.test.ts` — поломка craft → scrap, поломка drop → parts
7. Finalize: typecheck/lint/test/build; vitest зелёный; JS ≤ 2.5 MB. Anti-scope grep: нет правок в `src/scenes/`, `src/ui/`, `content/`, `assets/`.
8. Update `staff/status/ENGINEER.md`: M11.0a, DONE_PENDING_REVIEW.
9. Ready PR + Send Alex: «PR <ссылка>, готов к ревью PM».

## Forbidden

No `content/*.json`, no `assets/*`, no `docs/*`. No UI правки (`src/scenes/*`, `src/ui/*`). No third-party npm libs. No `any`. No self-merge. No новые механики кроме спеки.

## Deliverables checklist

- [ ] `src/types/items.ts` — все типы из §1 спеки
- [ ] `src/state/ItemRegistry.ts` — реализация + работает с пустым content (graceful fallback)
- [ ] `src/state/migrations.ts` — миграция v1 → v2 без потери данных
- [ ] `src/config.ts` — WEAPON_NAMING_MODE
- [ ] `src/systems/durability.ts` — поломка по правилам вопроса 4
- [ ] Тесты зелёные, покрытие новых файлов ≥ 80%
- [ ] `staff/handoff/M11.0a-ENG.md` — что реализовано, какие API, save migration notes
