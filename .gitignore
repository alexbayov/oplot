node_modules/
dist/
.cache/
*.log
.DS_Store
.env
.env.local
bun.lock

# Build artifacts / distributables (never commit — use Yandex Draft uploads instead)
*.zip
oplot_m*.zip

# Root-only local prototypes / scratch artifacts
/aquarium.html
